#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[pre-commit] Running cargo test..."
cargo test --quiet

echo "[pre-commit] Running janitor..."
cargo run --quiet --manifest-path janitor/Cargo.toml -- all
